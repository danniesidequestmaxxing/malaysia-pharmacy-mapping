streamlit>=1.35  # st.dataframe row-selection (`on_select`) used by the grid ranker
folium>=0.16
streamlit-folium>=0.20
pandas>=2.1
numpy>=1.26
shapely>=2.0
requests>=2.31
plotly>=5.20
pyarrow>=15      # DOSM also publishes parquet; keeps that path available.
pdfplumber>=0.11 # Parses the NPRA "farmasi-komuniti" premise-list PDF.
# Optional — only if you want to use the GeoPandas spatial-join path:
# geopandas>=0.14
